# Архитектурный паспорт локального контура (CASE-MACHERET-1997-2026)

* **Рабочее окружение и зеркалирование (`ACTOR_DEV_ENV`):**
  * **Основной локальный контур:** `H:\ACTOR_DEV_ENV` (разработка, скрипты, базы данных, отчеты).
  * **Облачное зеркало (аварийный слот):** `F:\Мой диск\ACTOR_DEV_ENV` (синхронизированный Google Диск для защиты от сбоев накопителей). Управляется скриптом `cloud_mirror_manager.py` с проверкой SHA-256 хэшей и поддержкой Robocopy / Rclone.

* **LLM-инфраструктура и шлюзы:**
  * **Мобильный мост (туннель):** `mobile_bridge.py` на порту `0.0.0.0:65434` (прием внешних запросов).
  * **Локальный узел исполнения:** `deepseek_task_node.py` на порту `127.0.0.1:65433` (модель `deepseek-coder:1.3b`).

* **Ядро Причинности и Рой Агентов (Causality Core):**
  * **Верификационный движок:** `causality_swarm_engine.py` и `causality_kernel.json`.
  * **Логика проверки:** Квантовые веса 1–9 (суперпозиция выживания и базовых стандартов) + императивный слой 10+ (ст. 53 и 64 Венской конвенции 1969 г., ВДПЧ 1948, UNCAT 1984). Возвращает статусы `AGENT_VALID` или `AGENT_COLLAPSE` (автоматическое аннулирование противоправных актов и коллизий).

* **Доказательный макрограф (`ECHR_IMPUNITY_DAG`):**
  * **Структура:** `ECHR_IMPUNITY_DAG.json` (узлы и рёбра) + `ECHR_IMPUNITY_DAG.dot` (исходник Graphviz).
  * **Визуализация:** `dag.png` (команда сборки: `dot -Tpng ECHR_IMPUNITY_DAG.dot -o dag.png`). Отображает связку *Jus Cogens* → иммунитеты ETS № 2 → UNCAT 1984 → проекция дела S-22.

* **Мультиязычная гармонизация (Ст. 85 Венской конвенции):**
  * Изоморфные JSON/YAML матрицы на английском, французском, испанском, русском и румынском языках. Сохраняют неизменные квантовые веса (`qubit_value: 0.111111`) и фиксируются в `multilingual_translation_manifest.json` с SHA-256 хэшами.
